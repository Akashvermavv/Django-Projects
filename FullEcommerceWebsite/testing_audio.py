# import pyttsx3# import PyPDF2# book = open('E:\Books/tutorial.pdf', 'rb')# pdfReader = PyPDF2.PdfFileReader(book)# pages = pdfReader.numPages## speaker = pyttsx3.init()# for num in range(30, pages):#     page = pdfReader.getPage(num)#     text = page.extractText()#     speaker.say(text)#     speaker.runAndWait()# a = [0,1,2,3,4,5]# for x in a:#     if x == 3:#         continue#     print(x)# +if(value.product_in_cart){<a class='btn btn-link' href ='/cart/'>In cart</a><button type='submit' class='btn btn-link'>Remove?</button>}else{<button type='submit' class='btn btn-success'>Add to Cart</button>}